{
  "name": "showdoc-vercel",
  "version": "1.0.0",
  "description": "ShowDoc adapted for Vercel deployment",
  "scripts": {
    "build": "cd web_src && npm install && npm run build",
    "dev": "cd web_src && npm run dev",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}